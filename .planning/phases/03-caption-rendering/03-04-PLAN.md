---
phase: 03-caption-rendering
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/cmds/caption.nim
  - src/main.nim
  - tests/unit.nim
autonomous: true

must_haves:
  truths:
    - "User can burn captions into video with `honeyclip caption burn`"
    - "User can export captions to NLE with `honeyclip caption export`"
    - "User can customize styling via CLI flags"
    - "User can enable word highlighting with --highlight flag"
  artifacts:
    - path: "src/cmds/caption.nim"
      provides: "Caption CLI command with burn and export subcommands"
      exports: ["runCaptionCommand"]
    - path: "src/main.nim"
      provides: "Caption command registration"
      contains: "caption"
  key_links:
    - from: "src/cmds/caption.nim"
      to: "src/render/captions.nim"
      via: "imports caption styling and filter generation"
      pattern: "import.*render/captions"
    - from: "src/main.nim"
      to: "src/cmds/caption.nim"
      via: "imports and dispatches to caption command"
      pattern: "import.*cmds/caption"
---

<objective>
Create CLI command for caption burning and NLE export with styling options.

Purpose: Provide user-facing interface for Phase 3 caption features. The `caption` command has two subcommands: `burn` for rendering captions into video, and `export` for generating NLE project files with caption tracks.

Output:
- `honeyclip caption burn` command with styling flags
- `honeyclip caption export` command with NLE format selection
- Integration with main.nim command dispatch
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-caption-rendering/03-RESEARCH.md
@.planning/phases/03-caption-rendering/03-01-SUMMARY.md
@.planning/phases/03-caption-rendering/03-02-SUMMARY.md
@.planning/phases/03-caption-rendering/03-03-SUMMARY.md
@src/cmds/transcript.nim
@src/main.nim
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create caption command module</name>
  <files>src/cmds/caption.nim</files>
  <action>
Create src/cmds/caption.nim with:

1. Imports:
   - std/[os, strutils, parseopt]
   - ../transcript/[types, grouping, formats, extract]
   - ../render/captions
   - ../exports/[fcp7, fcp11]
   - ../log
   - ../media

2. Help text constant with usage:
   ```
   Usage: honeyclip caption <subcommand> [options]

   Subcommands:
     burn    Burn captions into video
     export  Export captions to NLE project

   Common options:
     --input, -i <file>      Input video or JSON transcript
     --transcript, -t <file> Transcript JSON file (if input is video)
     --style <preset>        Style preset: traditional, modern (default: traditional)
     --highlight             Enable word-by-word highlight effect

   Burn options:
     --output, -o <file>     Output video file (required)
     --font <path>           Custom font file path
     --fontsize <size>       Font size in pixels (default: 60)
     --color <hex>           Text color (default: #ffffff)
     --position <pos>        Position: bottom, center, top (default: bottom)
     --outline               Enable outline (default: on for traditional)
     --no-outline            Disable outline
     --shadow                Enable drop shadow
     --box                   Enable background box
     --box-color <color>     Background box color (default: black@0.6)

   Export options:
     --output, -o <file>     Output project file (required)
     --format <fmt>          Export format: fcp7, fcpxml (default: fcp7)
     --absolute-paths        Use absolute media paths (default: relative)
   ```

3. parseCaptionStyle proc(args: seq[string]): CaptionStyle
   - Start with preset from --style flag (default "traditional")
   - Override individual settings from flags
   - Return configured CaptionStyle

4. runCaptionBurn proc(args: seq[string]): int
   - Parse input, output, transcript options
   - Load transcript from JSON or extract from video
   - Group into captions using groupIntoCaptions
   - Get video dimensions from input using getMediaInfo
   - Build CaptionBurnConfig from parsed style options
   - Call burnCaptions(inputPath, outputPath, captions, config) from render/captions
     (burnCaptions handles ASS generation, FFmpeg execution, and cleanup)
   - Return 0 on success (burnCaptions returned 0), 1 on error

5. runCaptionExport proc(args: seq[string]): int
   - Parse input, output, format options
   - Load transcript and group into captions
   - Based on format:
     - "fcp7": call fcp7_write_xml_with_captions
     - "fcpxml": call fcp11_write_xml_with_captions
   - Return 0 on success, 1 on error

6. runCaptionCommand* proc(args: seq[string]): int
   - If no subcommand or --help: show help
   - Dispatch to runCaptionBurn or runCaptionExport based on first arg
   - Return exit code
  </action>
  <verify>nim c --skipParentCfg --hints:off -o:nul src/cmds/caption.nim compiles without errors</verify>
  <done>Caption command module handles burn and export subcommands with styling options</done>
</task>

<task type="auto">
  <name>Task 2: Register caption command in main.nim</name>
  <files>src/main.nim</files>
  <action>
Update src/main.nim:

1. Add import at top:
   - import cmds/caption

2. Add "caption" to command dispatch:
   - In the main command switch/case:
   - Add case for "caption": return runCaptionCommand(args[1..^1])

3. Update help text to include caption command:
   - Add to command list: "caption   Burn captions into video or export to NLE"

Follow the existing pattern used for other commands (transcript, whisper, etc.)
  </action>
  <verify>nim c --skipParentCfg --hints:off -o:nul src/main.nim compiles without errors</verify>
  <done>Caption command registered and dispatched from main.nim</done>
</task>

<task type="auto">
  <name>Task 3: Add caption command tests</name>
  <files>tests/unit.nim</files>
  <action>
Add tests to tests/unit.nim:

1. Test parseCaptionStyle:
   - Default args returns traditional preset
   - --style=modern returns modern preset
   - --fontsize=72 overrides fontSize
   - --color=#ff0000 overrides color
   - --position=center sets cpCenter
   - --highlight enables highlightEnabled

2. Test runCaptionCommand help:
   - Empty args shows help (returns 0)
   - --help shows help (returns 0)

3. Test runCaptionCommand error cases:
   - Invalid subcommand returns error
   - Missing --input returns error
   - Missing --output for burn returns error

Note: Full integration tests require FFmpeg and video files.
These tests verify argument parsing and error handling.
  </action>
  <verify>nim c --skipParentCfg --hints:off -r tests/unit.nim runs tests successfully</verify>
  <done>Caption command tests verify argument parsing and error handling</done>
</task>

</tasks>

<verification>
1. Run: nim c --skipParentCfg --hints:off -o:nul src/cmds/caption.nim
   Expected: Compiles without errors

2. Run: nim c --skipParentCfg --hints:off -o:nul src/main.nim
   Expected: Compiles without errors

3. Run: nim c --skipParentCfg --hints:off -r tests/unit.nim
   Expected: All caption command tests pass

4. Manual verification (after nimble make):
   - honeyclip caption --help shows usage
   - honeyclip caption burn --help shows burn options
   - honeyclip caption export --help shows export options
</verification>

<success_criteria>
- `honeyclip caption burn` renders captions into video
- `honeyclip caption export` exports captions to NLE format
- Styling options (--font, --color, --position, etc.) work correctly
- --highlight flag enables word-by-word effect
- --style preset applies traditional or modern defaults
- Error handling for missing required options
- Help text documents all options
- Caption command accessible from main honeyclip binary
</success_criteria>

<output>
After completion, create `.planning/phases/03-caption-rendering/03-04-SUMMARY.md`
</output>
