---
phase: 03-caption-rendering
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cmds/caption.nim
  - src/exports/fcp7.nim
  - src/exports/fcp11.nim
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can export captions as FCP7 XML for Premiere/Resolve"
    - "User can export captions as FCPXML for Final Cut Pro"
  artifacts:
    - path: "src/exports/fcp7.nim"
      provides: "writeCaptionOnlyFCP7 function"
      exports: ["writeCaptionOnlyFCP7"]
    - path: "src/exports/fcp11.nim"
      provides: "writeCaptionOnlyFCPXML function"
      exports: ["writeCaptionOnlyFCPXML"]
    - path: "src/cmds/caption.nim"
      provides: "Wired runCaptionExport calling NLE functions"
  key_links:
    - from: "src/cmds/caption.nim"
      to: "src/exports/fcp7.nim"
      via: "writeCaptionOnlyFCP7 call in runCaptionExport"
      pattern: "writeCaptionOnlyFCP7\\("
    - from: "src/cmds/caption.nim"
      to: "src/exports/fcp11.nim"
      via: "writeCaptionOnlyFCPXML call in runCaptionExport"
      pattern: "writeCaptionOnlyFCPXML\\("
---

<objective>
Wire NLE caption export functions to CLI command

Purpose: Close the gap where addCaptionTrackFCP7 and addCaptionTrackFCPXML exist but are never called from the caption export command. The functions work; they just need to be connected.

Output: Working `honeyclip caption export` command that generates FCP7 or FCPXML files with caption tracks
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-caption-rendering/03-VERIFICATION.md

Key context from verification:
- addCaptionTrackFCP7() exists at src/exports/fcp7.nim line 264 (78 lines)
- addCaptionTrackFCPXML() exists at src/exports/fcp11.nim line 78 (109 lines)
- runCaptionExport() at src/cmds/caption.nim lines 330-340 has hardcoded errors
- These functions add caption tracks to existing XmlNode (video or spine)
- Need standalone writer functions that build minimal XML + add captions + write file
</context>

<gap_closure_rules>
CRITICAL: This is GAP CLOSURE. Do NOT:
- Recreate addCaptionTrackFCP7 or addCaptionTrackFCPXML (they exist and work)
- Add new caption track generation logic (it is done)
- Change the signature or behavior of existing functions

DO:
- Create thin wrapper functions that build minimal XML and call existing addCaptionTrack functions
- Wire runCaptionExport to call the new wrapper functions
- Remove the hardcoded error messages
- Keep changes minimal
</gap_closure_rules>

<tasks>

<task type="auto">
  <name>Task 1: Create standalone caption export function in fcp7.nim</name>
  <files>src/exports/fcp7.nim</files>
  <action>
Add a new proc `writeCaptionOnlyFCP7*` that:
1. Takes parameters: videoPath, captions, style, outputPath
2. Gets MediaInfo from videoPath for dimensions and framerate
3. Creates minimal FCP7 XML structure:
   - xmeml root (version="5")
   - sequence element
   - rate element (timebase from MediaInfo, ntsc computation)
   - media element
   - video element with format
   - Calls existing addCaptionTrackFCP7(video, captions, style, tb, ntsc)
4. Adds file reference for the video asset
5. Writes XML to outputPath (or stdout if "-")

Use the setTbNtsc helper that already exists for timebase/ntsc calculation.
Use elem() helper that already exists for creating elements.
Pattern after fcp7_write_xml but simpler (no audio tracks, no complex timeline).

Example signature:
```nim
proc writeCaptionOnlyFCP7*(videoPath: string, captions: seq[Caption], style: CaptionStyle, outputPath: string) =
```
  </action>
  <verify>Nim syntax check: `nim check src/exports/fcp7.nim` passes</verify>
  <done>writeCaptionOnlyFCP7 function exists and creates valid FCP7 XML structure with captions</done>
</task>

<task type="auto">
  <name>Task 2: Create standalone caption export function in fcp11.nim</name>
  <files>src/exports/fcp11.nim</files>
  <action>
Add a new proc `writeCaptionOnlyFCPXML*` that:
1. Takes parameters: videoPath, captions, style, outputPath
2. Gets MediaInfo from videoPath for dimensions and framerate
3. Creates minimal FCPXML structure:
   - fcpxml root (version="1.11")
   - resources element with format and asset
   - library/event/project structure
   - sequence with spine
   - Calls existing addCaptionTrackFCPXML(spine, captions, style, tb)
4. Adds asset-clip for video reference
5. Writes XML to outputPath (or stdout if "-")

Use getColorspace and makeName helpers that already exist.
Use pathToUri helper that already exists.
Pattern after fcp11_write_xml but simpler (caption-focused).

Example signature:
```nim
proc writeCaptionOnlyFCPXML*(videoPath: string, captions: seq[Caption], style: CaptionStyle, outputPath: string) =
```
  </action>
  <verify>Nim syntax check: `nim check src/exports/fcp11.nim` passes</verify>
  <done>writeCaptionOnlyFCPXML function exists and creates valid FCPXML structure with captions</done>
</task>

<task type="auto">
  <name>Task 3: Wire runCaptionExport to call NLE export functions</name>
  <files>src/cmds/caption.nim</files>
  <action>
Modify runCaptionExport() to replace the error stubs with actual function calls:

1. Remove the two error lines at lines 332-334 and 335-338:
   - `error "FCP7 export with captions not yet integrated..."`
   - `error "FCPXML export with captions not yet integrated..."`

2. Replace with actual calls:
   For fcp7 format:
   ```nim
   of "fcp7":
     writeCaptionOnlyFCP7(videoPath, captions, style, outputPath)
     conwrite("")
     echo &"Created: {outputPath}"
   ```

   For fcpxml format:
   ```nim
   of "fcpxml":
     writeCaptionOnlyFCPXML(videoPath, captions, style, outputPath)
     conwrite("")
     echo &"Created: {outputPath}"
   ```

3. Verify the imports at top of file include writeCaptionOnlyFCP7 and writeCaptionOnlyFCPXML (should already import from fcp7 and fcp11 modules).

4. Clean up the unused import warning by ensuring all imports are used (fcp7, fcp11, formats, extract, parseopt). The fcp7/fcp11 imports should now be used. Remove any truly unused imports.
  </action>
  <verify>Nim syntax check: `nim check src/cmds/caption.nim` passes</verify>
  <done>runCaptionExport calls writeCaptionOnlyFCP7 for fcp7 format and writeCaptionOnlyFCPXML for fcpxml format</done>
</task>

</tasks>

<verification>
After all tasks:
1. `nim check src/exports/fcp7.nim` - no errors
2. `nim check src/exports/fcp11.nim` - no errors
3. `nim check src/cmds/caption.nim` - no errors
4. Grep for "not yet integrated" in src/cmds/caption.nim returns no matches
5. Grep for "writeCaptionOnlyFCP7" in src/cmds/caption.nim returns match in runCaptionExport
6. Grep for "writeCaptionOnlyFCPXML" in src/cmds/caption.nim returns match in runCaptionExport
</verification>

<success_criteria>
- writeCaptionOnlyFCP7 function exists in src/exports/fcp7.nim
- writeCaptionOnlyFCPXML function exists in src/exports/fcp11.nim
- runCaptionExport calls writeCaptionOnlyFCP7 for fcp7 format
- runCaptionExport calls writeCaptionOnlyFCPXML for fcpxml format
- No "not yet integrated" error messages remain
- All files pass Nim syntax checking
</success_criteria>

<output>
After completion, create `.planning/phases/03-caption-rendering/03-05-SUMMARY.md`
</output>
