---
phase: 16-batch-processing-foundation
plan: 03
type: tdd
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - tests/unit.nim
autonomous: true

must_haves:
  truths:
    - "TOML template loads and converts to CLI args correctly"
    - "File discovery finds video files recursively and ignores non-video files"
    - "Output path generation preserves directory structure"
    - "Checkpoint round-trips through JSON serialization"
    - "Pending files calculation excludes completed and failed files"
  artifacts:
    - path: "tests/unit.nim"
      provides: "Unit tests for batch processing modules"
      contains: "batch template"
  key_links:
    - from: "tests/unit.nim"
      to: "src/batch/template.nim"
      via: "Tests import and verify template parsing and toArgs"
      pattern: "import.*batch/template"
    - from: "tests/unit.nim"
      to: "src/batch/checkpoint.nim"
      via: "Tests import and verify checkpoint serialization"
      pattern: "import.*batch/checkpoint"
---

<objective>
Add unit tests for the batch processing modules: template parsing, file discovery, output path generation, and checkpoint persistence.

Purpose: Verify the core batch processing logic works correctly with automated tests. Ensures template-to-args conversion, file discovery, path generation, and checkpoint round-tripping are reliable.

Output: `tests/unit.nim` (extended with batch tests)
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-batch-processing-foundation/16-01-SUMMARY.md
@.planning/phases/16-batch-processing-foundation/16-02-SUMMARY.md

# Source files to test
@src/batch/template.nim
@src/batch/discover.nim
@src/batch/checkpoint.nim

# Test file to extend
@tests/unit.nim
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add batch processing unit tests</name>
  <files>tests/unit.nim</files>
  <action>
Add unit tests for the batch processing modules to the existing `tests/unit.nim` file.

Add imports at the top of the file:
```nim
import ../src/batch/[template, discover, checkpoint]
```

Add a new test section for batch processing. Follow the existing test patterns in unit.nim (use whatever test framework/style is already established -- likely `unittest` or simple `assert` blocks).

**Test group: "Batch Template"**

1. **toArgs with all fields set**: Create a BatchTemplate with edit="audio", margin="0.2s", whenSilent="cut()", whenNormal="nil()", outputFormat="mp4", outputSuffix="_edited". Call `toArgs` and verify the result contains `["--edit", "audio", "--margin", "0.2s", "--when-silent", "cut()", "--when-normal", "nil()", "-ex", "mp4"]`.

2. **toArgs with empty fields omitted**: Create a BatchTemplate with only edit="audio" set (all others default). Call `toArgs` and verify it returns only `["--edit", "audio"]` -- empty fields should not generate args.

3. **toArgs with engage set**: Create template with engage="viral". Verify toArgs includes `["--engage", "viral"]`.

4. **validateTemplate with no issues**: Create a valid template. Verify `validateTemplate` returns empty seq.

**Test group: "File Discovery"**

5. **generateOutputPath with output dir**: Test `generateOutputPath("input/videos/test.mp4", "input", "output", "_edited", ".mp4")` returns `"output/videos/test_edited.mp4"` (or the platform-appropriate path separator equivalent).

6. **generateOutputPath without output dir**: Test `generateOutputPath("input/test.mp4", "input", "", "_edited", "")` returns `"input/test_edited.mp4"` (stays in same directory, keeps original extension).

7. **generateOutputPath preserves subdirectory**: Test with nested input `"input/a/b/c.mov"`, root `"input"`, output `"out"`, suffix `"_cut"`, ext `".mp4"` returns `"out/a/b/c_cut.mp4"`.

**Test group: "Checkpoint"**

8. **Checkpoint new and serialize**: Create a new checkpoint via `newCheckpoint("template.toml", "/videos", 10)`. Verify totalFiles is 10, completed is empty, failed is empty.

9. **markCompleted updates state**: Create checkpoint, call `markCompleted(state, "file1.mp4")`, verify `state.completed.len == 1` and `"file1.mp4" in state.completed`.

10. **markFailed updates state**: Create checkpoint, call `markFailed(state, "bad.mp4", "codec error")`, verify `state.failed.len == 1` and `state.failed[0].path == "bad.mp4"`.

11. **pendingFiles excludes completed and failed**: Create checkpoint with completed=@["a.mp4", "b.mp4"] and failed=@[FileResult(path: "c.mp4", error: "fail")]. Call `pendingFiles(state, @["a.mp4", "b.mp4", "c.mp4", "d.mp4", "e.mp4"])`. Verify result is @["d.mp4", "e.mp4"].

12. **Checkpoint JSON round-trip**: Create checkpoint, mark some completed/failed, save to temp file via `saveCheckpoint`, load via `loadCheckpoint`, verify all fields match. Use `getTempDir() / "test_checkpoint.json"` for the temp file. Clean up after test.

Run `nimble test` after adding all tests. All tests must pass.
  </action>
  <verify>Run `nimble test` -- all existing tests plus new batch tests must pass. Verify at least 10 new test cases cover template, discovery, and checkpoint modules.</verify>
  <done>Unit tests added for BatchTemplate.toArgs (populated and empty fields), generateOutputPath (with/without output dir, nested paths), CheckpointState (new, markCompleted, markFailed, pendingFiles, JSON round-trip). All tests pass via nimble test.</done>
</task>

</tasks>

<verification>
1. `nimble test` passes with all new batch tests
2. Template toArgs tests verify correct CLI argument generation
3. File discovery tests verify path generation with structure preservation
4. Checkpoint tests verify serialization round-trip and pending file calculation
5. No existing tests broken
</verification>

<success_criteria>
- At least 10 unit tests added covering template, discovery, and checkpoint modules
- toArgs correctly generates CLI arguments from template fields
- generateOutputPath preserves directory structure correctly
- Checkpoint serializes to JSON and deserializes identically
- pendingFiles correctly filters completed and failed files
- All tests pass via `nimble test`
</success_criteria>

<output>
After completion, create `.planning/phases/16-batch-processing-foundation/16-03-SUMMARY.md`
</output>
